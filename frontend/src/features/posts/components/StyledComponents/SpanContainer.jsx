import styled from 'styled-components';
import ContentContainer from '../../../../components/StyledComponents/ContentContainer';

const SpanContainer = styled(ContentContainer)`
  padding: 0;
  margin-left: 10px;
`;

export default SpanContainer;
