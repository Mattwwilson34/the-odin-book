import styled from 'styled-components';
import ContentContainer from '../../../../components/StyledComponents/ContentContainer';

const PhotoContainer = styled(ContentContainer)`
  display: flex;
  width: 100%;
  gap: 5px;
`;

export default PhotoContainer;
