import chalk from 'chalk';

// Shorten logs for brevity
const log = console.log;
const danger = chalk.red;
const warn = chalk.yellow;
const success = chalk.green;

export { log, danger, warn, success };
